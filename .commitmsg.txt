feat: 오일장 27→143곳 · 홈 339KB→133KB · 겨울 재고 매일 추적

2026-08-18 전체 점검의 «남은 것» 처리. 셋 다 손대기 전에 먼저 재봤고, 그 결과 계획이 바뀌었다.

1. 🏮 오일장 — 문제는 「5주째 미갱신」이 아니라 「27곳뿐」이었다
   markets.json 은 손으로 관리하는 27곳이고 좌표가 0건이라 지도·코스에 못 썼다.
   서울·부산·대구·광주·대전·세종·전북은 아예 0곳인데 제목은 「전국」이었다.
   → fetch-markets.js 신설: TourAPI 쇼핑>시장 cat3=A04010100 에서 143곳(전부 좌표 있음).
     추가 활용신청 불필요 — 카페·명산과 같은 채널이다.
   → 장날은 TourAPI에 전용 필드가 없어 제목 괄호와 개요 문장에서 뽑았다.
     ⚠️ 패턴을 늘리는 대신 **끝자리 간격이 정확히 5일 때만** 인정하는 규칙을 뒀다.
        그래서 「23, 28일에 장」도 3·8일장으로 바르게 읽고, 「5, 15, 25일」(10일장)은 거른다.
        추출률 첫 시도 63% → 「(4일, 9일)」·「장날은 매월 2일, 7일이다」를 놓친 것을 고쳐 87%.
   → 손큐레이션 27곳은 대표 품목·특징 서술이 좋아 이름이 겹치면 그쪽을 살린다.
     「경기 안성장」↔「안성장」처럼 지역 접두어가 붙은 중복은 접두어를 뗀 이름으로 한 번 더 맞춘다.
   → 결과: 장날 확인 143곳(표) + 장날 미확인 19곳(별도 목록으로 정직하게 구분).
     시·도별 분포와 끝자리별(1·6/2·7/3·8/4·9/5·10) 묶음 섹션 신설. 본문 15,623자.
   ⚠️ 장날을 못 뽑은 곳은 표에 넣지 않는다 — 표의 JS가 빈 배열을 「상설」로 표시하기 때문에
      모르는 것을 안다고 말하게 된다.

2. ⚡ 홈 339KB → 133KB (-61%), /test/ 396KB → 84KB (-79%)
   홈 445KB의 정체는 인라인 스크립트 하나(228KB)였다. 사람이 읽는 본문은 6KB뿐이었고,
   같은 배열을 /test/ 도 통째로 안고 있었다.
   → /fest.json 으로 분리. 그런데 그 배열의 58%가 `near`(근처 관광지)로, 모달을 열어야 쓰는 값이
     첫 화면 렌더를 기다리게 하고 있었다 → /fest-near.json 으로 한 번 더 쪼개
     카드를 다 그린 뒤 배경으로 받아 같은 객체에 합친다.
   검증: 생성된 인라인 스크립트 92개 전부 문법 검사 통과.

3. ❄️ 겨울 절벽 — 매일 재고를 남기고 임계치에서 알린다
   대시보드가 이미 색인 수는 남기고 있었지만, 색인은 «결과»이고 재고가 «원인»이다.
   → _cjm_dashboard.py 에 앞으로 30/60/90/180일 안에 열리는 축제 수를 스냅샷에 추가하고
     카드로 표시. 30일 재고가 40건 아래로 떨어지면 「지금 봐야 할 것」 맨 위에 경보를 띄운다.
   현재값: 30일 107 · 60일 186 · 90일 203 · 180일 206 (끝난 것 709/915) — 아직 경보 없음.

📌 데스크톱 CTR(1.86% vs 모바일 5.39%)은 이번에 손대지 않았다.
   기기×순위로 쪼개니 원인이 제목이 아니라 **순위**였다(데스크톱 평균 10.4위 vs 모바일 7.6위,
   11위 밖 노출이 202 vs 99). 게다가 데스크톱 노출의 큰 덩어리가 오늘 이미 조치한 것들이다
   — 추석 제목 교체(214노출·8.4위·클릭0)와 영어 페이지 noindex(49~73위).
   두 조치의 효과가 반영될 2주 뒤에 다시 재는 게 맞다.
