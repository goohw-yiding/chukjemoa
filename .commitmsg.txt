fix: 코스 필드 충돌로 죽어 있던 무장애/반려 가산점 복구 + 코스 12페이지 고유 본문 보강

- course-data.js: acc/pet 레코드의 'cat' 필드가 엔진의 o.cat(카테고리)를 덮어써
  라벨이 'undefined'로 출력되고(7페이지 21곳) 휠체어/반려견 선호 +3점이 아예 안 걸렸다.
  필드명을 sub로 변경. render.js에 라벨 폴백 추가.
- engine.js: 「한적한 곳」 감점을 배수 원값 대신 시도 안 순위로 환산.
  전남 배수 폭이 1.01~1.15뿐이라 감점 최대 0.6 → 사진 가산점(1.6)에 눌려
  「전남 한적한 코스」가 전남에서 가장 붐비는 여수시를 고르고 있었다.
  이제 담양군(8위)·장성군(11위)·영광군을 고른다.
- course.js: 코스별 고유 섹션 3종 신설(이 코스를 숫자로 보면 / 이달 붐빔 순위 /
  근처 오일장 일정), FAQ 2개를 코스 실제 숫자로 생성, AI 예시질문을 코스 조건에 맞춤
  (당일치기 페이지에 "2일차가 빡빡해요"가 떠 있었다).
  → 12페이지 8-gram 자카드 0.536 → 0.471, 본문 평균 3,443 → 4,473자.
  (배경: 코스 13페이지 중 색인 1개. 축제 상세 유사도는 0.195인데 코스만 0.536이었다)
