일문 장소 한글주소 649건 0원으로 채움 + 역지오코딩 게이트가 «이름매칭을 덮어쓰던» 것 수정

places_ja 주소 없음 666 → 17건 (99.5%). 새로 산 것 0건 — 이미 사 둔 캐시
(C:\dev\ncp_maps\revgeo_cache.json 3,298건)에 653건이 그대로 있었다.

🔴 그런데 그대로 --write 했으면 손해였다.
   revgeo_gate.js 가 `r.addrKo = g.full` 로 «무조건» 덮어쓰고 있었다.
   이 파일 주소의 대부분(2,605건)은 match-addr-ko.js 가 «이름으로» 이은 것이고,
   그게 역지오코딩보다 낫다 — 그 장소의 «등록된» 주소이지 좌표 근처의 주소가 아니다.
   (2026-09-02에 일부러 역지오코딩을 이름매칭으로 덮어썼던 그 방향이다)
   → 비어 있는 곳만 채우도록 고치고, 「새로 채움 / 건드리지 않음」을 따로 세어 보고하게 했다.
   결과: 새로 채움 649 · 그대로 둔 것 2,605.

절차 확인도 같이 했다 — `match-addr-ko.js places_ja.json` 를 먼저 돌렸으나 신규 0건
(이름으로 이을 수 있는 건 이미 다 이어져 있었다). 동명이지 7건은 게이트가 버렸다
(グラスハウス 강원↔제주 556km, 天津港 549km 등).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01T1Q5u3iQj8yz2GzSyEhrZb
