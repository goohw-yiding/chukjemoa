# 2026-09-11 작업 인수인계

> 이 파일은 배포되지 않는다(.vercelignore 의 `/*.md`). 다음 세션이 먼저 읽을 것.

## 한 줄 요약

박물관·전시공간 3페이지를 냈고, **웹푸시 자동 발송을 관심사·지역별로 갈라서 완성**했다.
첫 발송은 **2026-09-12(토) 08:00** 에 자동으로 나간다.

---

## 1. 새 페이지 3장 (배포 완료)

| URL | 내용 | 정렬 기준 | 본문 |
|---|---|---|---|
| `/seoul/museum/` | 박물관·미술관 114곳 | 네이버 월 검색량 | 6,659자 |
| `/busan/museum/` | 22곳 | 검색량 | 4,640자 |
| `/seoul/venue/` | 전시 공간 242곳 | 실제 전시 개최 횟수 | 6,824자 |

- 코드: `museum.js` (3페이지 모두 여기서 만든다). `build.js` 2158행 근처에서 require.
- 데이터: `data/museum_volume.json`(`_museum_volume.py`), `data/seoul_culture.json`(`fetch-seoul-venue.js`)
- 제주는 `jeju.js` 가 제주 전용 API 로 따로 짓는다 — **건드리지 말 것**.

### 지어 놓고 세어 보고 고친 것 2가지

1. **무장애 정보가 136곳 모두 0건**이었다. `fetch-accessible.js` 가 `detailWithTour2` 를 이미 다 조회했고
   `enr=1` 이다 — 「못 받은」 게 아니라 「공공데이터에 없는」 것이다. 그런데 제목·설명·머리글·FAQ가
   전부 "무장애 정보 있음"이라 말하고 있었다. `withAcc` 가 0이면 그 말이 통째로 빠지게 고쳤다.
2. 대신 안 쓰고 있던 **관광공사 개요(`ov`, 135/136곳)** 를 카드에 붙였다.
3. 라이브에서 「최근 2027-08-29」 — `last` 는 마지막 전시의 **종료일**이라 미래 날짜가 된다.
   오늘보다 뒤면 "예정 ~", 앞이면 "최근" 으로 갈랐다.

### `/seoul/venue/` 를 따로 뺀 이유

`/seoul/exhibition/`(seoul.js)이 **이미** 「지금 하는 전시 105건」을 본문 17,259자로 싣고 있다.
같은 걸 또 만들면 중복 페이지다. `/seoul/venue/` 는 행사가 아니라 **장소 명부**다.
`/museum/` 안에는 상위 10곳만 두고 242곳 전체는 `/seoul/venue/` 로 보낸다.

---

## 2. 🔴 내가 틀렸던 것 — OneSignal 은 이미 깔려 있었다

`build.js` 를 「구독·뉴스레터·이메일·카카오·알림」으로 grep 해 0건이 나오자
"발송 시스템 없음"이라고 단정했다. **정작 깔려 있던 건 그 단어가 하나도 안 들어가는 OneSignal 이었다.**

- `build.js` 2010행에 OneSignal SDK 가 전 페이지에 들어간다
- 루트 `OneSignalSDKWorker.js` 라이브 200 — 진짜 서비스워커
- `.vercelignore` 가 `/*.js` 를 막고 `!/OneSignalSDKWorker.js` 로 그것만 되살려 둔다

**교훈: grep 0건은 「없다」가 아니라 「그 단어로는 못 찾았다」이다.**
새로 만들기 전에 루트 파일 목록과 `.vercelignore` 를 먼저 읽을 것.

(이 오해로 `/pushtest/` 를 지었다가 전부 되돌렸다 — 커밋 `ff6ec12a`.
 서비스워커를 scope `/` 로 또 등록하면 기존 구독이 깨진다.)

---

## 3. 웹푸시 자동 발송 (핵심 작업)

### 대시보드에서 확인한 사실 (2026-09-11)

- OneSignal 앱 `축제모아` · appId `8d4d29df-1dba-4f43-9efb-0c3745441e1f` · Free 플랜
- **웹푸시 구독자 54명**, 전원 구독 상태. 9/7~9/11 나흘 사이에 붙었다
- **지금까지 보낸 알림 0건** — 모아만 놓고 한 번도 안 썼다
- 구독자 기기는 Windows 다수 + Android(Linux armv81) 일부
- 웨일 걱정은 풀렸다: 방문자 절반이 웨일인데 54명이 실제로 등록됐다

### 왜 관심사별로 가르나

GA4 9/7~9/11(구독자가 붙은 바로 그 나흘) 랜딩 페이지 1,599세션:

| 처음 들어온 페이지 | 비중 |
|---|---|
| 월별 축제 | 41.8% |
| 오일장 | 31.3% |
| 홈 | 11.3% |
| 블로그 | 6.1% |
| 축제 상세 | 4.1% |

오일장 안에서도 경북 42 · 충남 39 · 전북 36 · 경기 27 · 충북 26 · 전남 26 으로 갈린다.
**전남 사람에게 「경북에 장 섭니다」를 보내면 그게 차단이다.** 웹푸시는 한 번 차단당하면 되돌릴 방법이 없다.

### 심은 태그 6개 (`build.js` 2010행)

무료 플랜 Data Tag 한도가 **정확히 6개**다. 더 넣지 말 것.

| 태그 | 값 | 쓸 곳 |
|---|---|---|
| `interest` | jangteo / festival / blog / nature / city | 관심사 분기 |
| `region` | 시·도 슬러그 | 「오늘 전남에 장 서는 곳」 |
| `fest` | 본 축제 슬러그 | 그 축제 D-3 |
| `month` | 본 월별 페이지 | 다음 달 나왔을 때 |
| `lang` | ko/en/ja/es/zh/tw | 외국어 분리 |
| `seen` | 마지막 방문일 | 죽은 구독 정리 |

- **마지막 한 페이지로 사람을 규정하지 않는다.** `localStorage['cjm_int']` 에 방문을 누적해
  «가장 많이 본 것»을 관심사로 삼는다.
- ⚠️ **정규식을 쓰지 않았다.** 이 코드는 `build.js` 의 템플릿 리터럴 안이라 `\/` 가 `/` 로 풀려
  정규식 리터럴이 통째로 깨진다. `split` 과 배열 `indexOf` 로만 갈랐다. **고칠 때 정규식 넣지 말 것.**
- 라이브 실측 확인: 경북 오일장 → 10월 축제 순으로 열었더니
  `{interest:jangteo, region:gyeongbuk, month:2026-10, lang:ko, seen:2026-09-11}`

### 발송기 `_push.js`

```
node _push.js jangteo            미리보기 (기본 — 아무것도 안 나간다)
node _push.js jangteo --send     실제 발송
node _push.js festival [--send]
node _push.js month [--send]
```

- **세그먼트를 대시보드에서 만들지 않는다.** REST API 의 `filters` 로 보낼 때 태그를 건다 —
  시·도 17개 × 관심사마다 세그먼트를 만들 필요가 없고, 지역이 늘어도 손댈 데가 없다.
- 재료는 `build.js` 가 매일 새로 쓰는 것 그대로: `market-fav.json`(467곳) ·
  `fest.json` · `fest-live.json`. 따로 수집할 게 없다.
- ⚠️ `dn`(장날)의 `10` 은 **끝자리 0** 을 뜻한다(literal 10). `%10` 으로 비교한다.
  `build.js` 2809행이 같은 함정을 주석으로 남겨 뒀다.
- ⚠️ 시장 이름을 줄이지 않는다 — 「광양5일시장」에서 시장을 떼면 「광양5일」이 되어 뜻이 사라진다.
  처음에 그렇게 만들었다가 미리보기에서 보고 되돌렸다.
- ⚠️ 문구는 **정보성**이다. 할인·판매를 넣는 순간 광고성이 되어 제목에 `(광고)` 와
  수신거부 안내가 필요하다(정보통신망법).

### 과다 발송 자물쇠 `_push_log.json`

같은 대상에 **오일장 60시간 · 축제 120시간 · 월간 480시간** 안에는 다시 안 보낸다.
빈도 초과가 웹푸시의 가장 흔한 사망 원인이다. 한 사람은 `interest` 태그가 하나뿐이라
두 흐름을 겹쳐 받지도 않는다. **이 파일을 지우면 중복 발송이 난다.**

### 예약 작업 3개

| 작업 | 시점 | 왜 그 시각인가 |
|---|---|---|
| `chukjemoa-push-jangteo` | 수·토 08:00 | 07:00 빌드 뒤라야 오늘 데이터다 |
| `chukjemoa-push-festival` | 목 18:00 | 주말 계획을 세우는 시점 |
| `chukjemoa-push-month` | 매일 09:00 | 자물쇠(480시간)가 월 1회만 통과시킨다 |

- 등록 스크립트 `_push_tasks.ps1` (다시 돌리면 덮어쓴다)
- ⚠️ `.ps1` 은 **ASCII 전용** — 한글을 넣으면 파서가 죽는다(`TerminatorExpectedAtEndOfString`)
- 로그: `C:\dev\_push_chukjemoa.txt`

### 🔑 `onesignal.key`

- `os_v2_app_` 로 시작하는 113자. `*.key` 라 git 제외 — **`_d2p.js` 로 양쪽 폴더에 다 있어야** 한다
  (seoul.key·busan.key 와 같은 함정. 한쪽에만 있으면 예약작업이 조용히 0건이 된다)
- ⚠️ 이 키는 2026-09-11 세션에서 **대화 기록에 노출됐다**(클립보드 복사가 두 번 실패해
  화면 캡처로 확인하는 과정에서). 장남 님이 걱정되시면 대시보드에서 Disable 하고
  새로 만들어 직접 파일에 붙여넣는 게 낫다. 사용자에게 이미 알렸다.
- 인증 확인 완료: `HTTP 200` + `{"errors":["All included players are not subscribed"]}`
  → 키는 정상, 아직 태그 붙은 사람이 0명이라 아무것도 안 나갔다(정상)

---

## 4. 유튜브 채널 (설정 완료)

- 채널 `UCI_eeM6hq4P1O5BqoOpll3A` · `@축제모아`
- 채널 설명 376자 + 홈페이지 링크 게시 완료
- 배너·프로필 업로드 완료. **사이트와 같은 얼굴로 맞췄다** —
  프로필은 `favicon.svg` 와 **똑같은 도형**(주황 #E0502F 바탕 + 흰 폭죽, 8방향 살·끝점·가운데 원 좌표 동일),
  배너는 사이트 헤더를 키운 배치(흰 바탕 + 주황 마크 + 청록 「축제모아」)
- 원본: `C:\dev\chukjemoa\_ytart\` (avatar2.png 800×800 / banner2.png 2560×1440)
- ❌ 채널 키워드는 못 넣었다 — 설정 대화상자가 안 열렸다. 유튜브가 오래전에 비중을 줄인 항목이라 급하지 않다
- 영상 0개 — 여기는 아직 비어 있다

---

## 5. 다음에 확인할 것

| 날짜 | 무엇 |
|---|---|
| **2026-09-12 08:00** | 첫 오일장 발송. `C:\dev\_push_chukjemoa.txt` 에 「받는 사람 N명」 |
| **2026-09-17 18:00** | 첫 축제 발송 |
| 2026-09-15 무렵 | 로그를 보고 판단: N이 계속 0 → 태그가 안 붙는다(코드 문제) / N은 나오는데 클릭 0 → 문구 문제 / 구독자 감소 → 빈도 문제 |
| 2026-09-17 | 8/18 noindex·제목 조치 재크롤 확인 |
| 2026-09-22~ | 옥토버페스트 서울 2026 GSC 반영 |
| 2026-09-23 | 시·군 장날 88장 효과 · `/maple/` 재겨냥 · 진주남강유등축제 네이버 발행 예약 |
| 2026-09-29 | 영문 제목 조치 효과 |

### 아직 안 한 것

- **구독자 증가 경로를 안 봤다.** 구독자 목록 첫 페이지에 Windows 가 눈에 띄게 많았는데
  사이트 트래픽은 모바일 76% 다. 사실이라면 모바일에서 구독 안내가 제대로 안 뜨고 있다는 뜻이다.
  OneSignal 대시보드의 프롬프트 설정(Settings > Web Configuration)을 아직 못 열어 봤다.
- 이메일·카카오는 손대지 않았다. 브라우저 알림 결과를 보고 정하기로 했다.
