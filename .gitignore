.vercel
vc.js
.vercel
tourapi.key
*.key
node_modules/
_*.js
# ⚠️ 2026-09-15 — 위 한 줄이 «예약작업이 매일 돌리는 스크립트»까지 같이 막고 있었다.
#   _daily.ps1 은 추적되는데 그 안에서 부르는 _weekly_fetch.js·_sync.js·_d2p.js 는 이 PC 에만
#   있었다. 디스크가 죽으면 자동화가 통째로 사라진다. 진단용 _*.js 는 계속 막고, «도는 것»만 뺀다.
!_weekly_fetch.js
!_slow_fetch.js
!_sync.js
!_d2p.js
.fuse_hidden*
apikeys.json
apikeys.local.json
_*.log
# 커밋 메시지 임시 파일 — 네 번 딸려 들어갔다. 패턴으로 막는다.
_*.txt
# 카드뉴스 렌더 결과물 — 배포도 안 되고(.vercelignore) 회차마다 쌓이므로 추적 안 함(2026-08-27)
cardnews/out/
# 네이버 발행 파이프라인의 «산출물» — 도구(naver-queue.js·naver-brief.js)만 추적하고
# 매번 다시 만들어지는 큐·브리프·검색량 캐시는 추적하지 않는다(2026-08-31).
naver-queue.md
naver-brief-*.md
audit-buybox.md
audit-busy.md
audit-supply.md
audit-newdata.md
data/naver-queue.json
data/naver-kwcache.json
# 진단·백업용 임시 JSON — 1.8MB 짜리 places_ja 백업이 커밋에 딸려 들어갈 뻔했다(2026-09-02).
#   _*.txt / _*.js / _*.log 는 이미 막고 있었는데 .json 만 뚫려 있었다.
_*.json
# 2026-09-01 일회성 진단 산출물 — 어떤 코드도 읽지 않는다.
data/thin_intl.json
data/thin_intl_age.json
# 날씨 예보 — 매일 fetch-weather.js 가 새로 만든다. 커밋하면 «어제 예보»가 배포에 섞인다(2026-09-04).
data/weather.json
_*.err 
# 진단 «산출물» — 도구(_*.js)는 이미 막고 있었는데 결과 파일만 뚫려 있었다(2026-09-11).
#   _prod_map.md · _bb_real.md · _fallback_slugs.tsv 가 커밋에 딸려 들어갈 뻔했다.
_*.md
_*.tsv
